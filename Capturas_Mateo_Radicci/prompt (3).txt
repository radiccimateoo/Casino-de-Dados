/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package ie_interfaz_grafica;
import java.util.*;

/**
 *
 * @author usuario
 */
public class IE_INTERFAZ_GRAFICA {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        
        Scanner scanner = new Scanner(System.in);
        Casino casino = new Casino();

        System.out.println(" ¡Bienvenidos al Casino de Dados! ");

        // Crear jugadores
        int n;
        while (true) {
            System.out.print("¿Cuántos jugadores participarán? (2-4): ");
            if (scanner.hasNextInt()) {
                n = scanner.nextInt();
                scanner.nextLine(); // limpiar el \n
                if (n >= 2 && n <= 4) break;
                System.out.println("Número inválido. Debe ser 2, 3 o 4.");
            } else {
                System.out.println("Entrada inválida. Ingrese un número.");
                scanner.nextLine(); // descartar lo que se haya escrito
            }
        }


        for (int i = 1; i <= n; i++) {
            System.out.print("Nombre del jugador " + i + ": ");
            String nombre = scanner.nextLine();

            System.out.print("Tipo (1=Novato, 2=Experto, 3=VIP): ");
            int tipo = scanner.nextInt();
            scanner.nextLine();

            Jugador jugador = casino.crearJugador(nombre, tipo);
            casino.agregarJugador(jugador);
        }
        
        /*
        aca implementamos primera mejora, se entiende que por cada partida hay x
        cantidad de rondas, para hacerlo mas rapido establecemos como valores
        fijos 8 partidas y 3 rondas por partida, para hacer un historial largo
        */
        
        /*
        System.out.print("\n Ingrese la cantidad de partidas que quiere jugar: ");
        int cantPartidas = scanner.nextInt();
        scanner.nextLine();
        */
        
        System.out.println("\n*************************************");
        System.out.println("\nComienza el juego!");
        casino.jugar(8);

        scanner.close();
    }
    
}


bien ahora que ya devolvemos la lista, tenemos que tratarla en la clase main para poder guardar la partida con el detalle, por lo tanto el metodo casino.jugar() tiene que estar guardado en una variable  de tipo lista para poder obtener los datos que son retornados desde la clase casino con dicho metodo, recorrer esa lista e iterar por cada detalle y con el metodo guardar partida vamos registrando en un archivo txt los detalles de cada partida recoorda que tenes que trabajar con stringBuilder

para el metodo guardarPartida(String detalle) y guardar la partida en un txt usa FileWriter, en este caso vamos a guardar todas las partidas para que queden registro siempre de todas y se vayan agregando todas, luego apra elmetodo  mostrarHistorial() vamos a recorrer el archivo y mostrar los ultimos 5 que es el pedido de la consigna pero por ahora vamos a crear solaemnte el metodo guardarPartida(String detalle)
bien, ahora tengo que mostrar el detalle, como mi logica esta implemtanda en la clase casino y lo tengo que implementar en la clase main al detalle haremos lo siguiente, en mi clase casino ademas de mostrar el mensaje en pantalla por conosla gauarda en una variable para ser retornada y utilizada en la clase main junto con el metodo guardarPartida(String detalle) en el que traiga los jugadores, el ganador de esa partida y la cantidad de rondas jugadas en total qyue siempre va a ser 3, luego vemos como armar el metodo guardar partida en main, pero ahora vamos con el pedidoe nla clase casino: 


package ie_interfaz_grafica;

/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
import java.util.ArrayList;
/**
 *
 * @author usuario
 */
public class Casino {
    
    private ArrayList<Jugador> jugadores;

    public Casino() {
        jugadores = new ArrayList<>();
    }

    public Jugador crearJugador(String nombre, int tipo) {
        int dineroInicial = 500; // Todos empiezan con $500
        switch (tipo) {
            case 1 -> {
                return new JugadorNovato(nombre, dineroInicial);
            }
            case 2 -> {
                return new JugadorExperto(nombre, dineroInicial);
            }
            case 3 -> {
                return new JugadorVIP(nombre, dineroInicial);
            }
            default -> {
                System.out.println("Tipo inválido, se asignará como Novato.");
                return new JugadorNovato(nombre, dineroInicial);
            }
        }
    }

    public void agregarJugador(Jugador jugador) {
        jugadores.add(jugador);
    }
    
    /* por x cantidad de partidas siempre se jeugan 3 rondas*/
    public void jugar(int cantPartidas) {
        for (int i = 1; i <= cantPartidas; i++) {
            System.out.println("\n=== Partida " + i + " ===");

            // Siempre 3 rondas fijas
            JuegoDados juego = new JuegoDados(jugadores);
            for (int r = 1; r <= 3; r++) {
                System.out.println("\n----Ronda " + r);
                juego.jugarRonda();
            }

            // Mostrar estado de los jugadores después de la partida
            System.out.println("\nEstado después de la partida " + i + ":");
            for (Jugador j : jugadores) {
                System.out.println(j.getNombreConTipo() + " - Dinero: $" + j.getDinero());
            }

            // Verificar si alguien se quedó sin dinero
            boolean alguienSinDinero = false;
            for (Jugador j : jugadores) {
                if (j.getDinero() <= 0) {
                    alguienSinDinero = true;
                    break;
                }
            }
            if (alguienSinDinero) {
                System.out.println("\nUn jugador se quedó sin dinero. ¡Fin del juego!");
                break;
            }
        }

        // Anunciar ganador final
        Jugador ganador = jugadores.get(0);
        for (Jugador j : jugadores) {
            if (j.getDinero() > ganador.getDinero()) {
                ganador = j;
            }
        }
        System.out.println("\n¡El ganador final es " + ganador.getNombreConTipo() + " con $" + ganador.getDinero() + "!");
    }
    
}
